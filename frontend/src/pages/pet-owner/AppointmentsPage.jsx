export default function AppointmentPage(){
    return(
        <main>
            <div>
                This is the page for recent and upcoming appointments.
            </div>
        </main>
    );
}